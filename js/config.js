// --- Cấu hình chung ---
const CONFIG = {
    API_BASE_URL: 'http://localhost:8080/api/v1',
    
    // Thời gian chờ tối đa(milliseconds)
    TIMEOUT: 10000,
    
    // Biến bật/tắt chế độ dùng dữ liệu giả mạo (Mock Data)
    // - true: Dùng dữ liệu giả 
    // - false: Gọi API thật lên server
    USE_MOCK_DATA: true
};
